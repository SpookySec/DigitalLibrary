#include "LibraryUser.h"

// Returns the name of the library user
string LibraryUser::getName() {
    return name;
}

// Returns the user ID of the library user
int LibraryUser::getUserId() {
    return user_id;
}
